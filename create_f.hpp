#include <string>


/// @brief Class to create a new project
/// @author Thibaud VINCENT
class Create {
	public:
		/// @brief To create a new project and setup files
		/// 
		/// @params m_path The path where we will create the new project
		Create(std::string m_path);
		~Create();
};
